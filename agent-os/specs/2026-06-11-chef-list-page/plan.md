# Chef List Page Implementation Plan

**Goal:** Build the /chefs list page — 3-column grid of chef cards with All / New / Most Viewed tabs.

**Architecture:** app/chefs/page.tsx server component → ChefsGrid client component → ChefCard in ui-components.

**Tasks:**
1. Save spec documentation (this task)
2. ChefCard component in ui-components (TDD)
3. TEXT strings + ChefsGrid component
4. /chefs page + SCSS styles
